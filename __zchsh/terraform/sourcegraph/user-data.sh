#!/bin/bash
#
# TODO: aiming to eventually set up instance etc propertly,
# so that I could use the full `user_data` SourceGraph needs:
# https://docs.sourcegraph.com/admin/install/docker-compose/aws
#
echo "Hello Terraform!"
touch testfile.txt
